{
  "name": "pokermentor-functions",
  "description": "Serverseitiger Entitlement-Service und Zahlungs-Webhooks",
  "private": true,
  "type": "commonjs",
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
